function TimerSyncer() {
    const clickHandler = () => {
        
    }

    return(
        <div>
            <button onClick={clickHandler}>Sync</button>
        </div>
    )
}

export default TimerSyncer;